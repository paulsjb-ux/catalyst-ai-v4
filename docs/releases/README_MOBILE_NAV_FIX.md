# Mobile Navigation Fix

On phones and tablets, the navigation now wraps into a fixed grid instead of sliding horizontally. This prevents the selected tab from overlapping or covering neighbouring buttons.
