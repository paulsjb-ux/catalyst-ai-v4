# Catalyst AI v14.5.1 — Navigation hotfix

This patch fixes desktop/sidebar navigation being reverted by stale mobile navigation state. Both navigation controls now update a shared canonical route through callbacks. It also makes the 30-day validator explicitly show when it is waiting for the next eligible trading day.
